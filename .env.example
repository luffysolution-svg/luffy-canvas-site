# Netlify: set this in Site configuration > Environment variables with Functions scope.
# Exact, comma-separated provider-owned hostnames used by remote image result URLs; wildcards (*) are unsupported.
IMAGE_PROXY_ALLOWED_HOSTS=
